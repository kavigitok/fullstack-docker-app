name: Docker Build Test and Push

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  docker-build-test-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Validate Docker Compose
        run: docker compose config

      - name: Build Compose services
        run: docker compose build

      - name: Start containers
        run: docker compose up -d

      - name: Wait for services
        run: sleep 30

      - name: Show running containers
        run: docker compose ps

      - name: Test Nginx reverse proxy home endpoint
        run: curl -f http://localhost:8080/

      - name: Test backend health endpoint through Nginx
        run: curl -f http://localhost:8080/health

      - name: Test backend readiness endpoint through Nginx
        run: curl -f http://localhost:8080/ready

      - name: Test backend database endpoint through Nginx
        run: curl -f http://localhost:8080/db

      - name: Stop containers
        if: always()
        run: docker compose down

      - name: Login to Docker Hub
        if: github.event_name == 'push'
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build backend image for Docker Hub
        if: github.event_name == 'push'
        run: docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/fullstack-backend:latest ./backend

      - name: Push backend image to Docker Hub
        if: github.event_name == 'push'
        run: docker push ${{ secrets.DOCKERHUB_USERNAME }}/fullstack-backend:latest
